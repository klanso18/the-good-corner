export type Ad = {
  title: string;
  description: string;
  owner: string;
  price: number;
  location: string;
  picture: string;
  createdAt: string;
  id: number;
}

export type Category = {
  name: string;
  id: number;
}